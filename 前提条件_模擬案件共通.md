# 模擬案件 共通前提条件

> ポートフォリオ用模擬案件を構築する際の共通ルール。
> Kakeru（確定申告アプリ）開発時の実績をベースに更新。

---

## 技術スタック

### フロントエンド・バックエンド
- Next.js 16（App Router）※ Next.js 14で作成したプロジェクトも16で動作
- TypeScript
- Tailwind CSS v4
- NextAuth.js（認証）

### データベース・ストレージ
- Supabase（PostgreSQL）
- **Prismaは使用しない**
  - 理由：日本の一般家庭回線はIPv4のため、SupabaseへのTCP直接接続ができない
  - 代替：`@supabase/supabase-js` クライアントを使用
  - テーブル作成はSupabase MCP経由 または SQL Editorで行う
- Supabase Storage（ファイルアップロード）
- **RLSポリシーは各テーブル作成時に必ず設定する**
  - 公開フォームからのINSERT：`anon` ロールに許可
  - 管理画面からのCRUD：`authenticated` ロールに許可

### メール送信
- Resend（無料：100通/日）
- 送信元：`onboarding@resend.dev`（独自ドメイン不要）
- 送信先はResend登録メールアドレスのみ（開発環境）

### PDF生成
- `window.print()` ＋ 印刷用CSS（日本語文字化け回避のため）
- jsPDFは日本語フォント埋め込みが必要で複雑なため非推奨

### ホスティング
- Vercel（無料プラン）
- GitHub連携で自動デプロイ

---

## Supabase 運用ルール（重要）

### プロジェクト共有・プレフィックス方式
- Supabase無料プランはプロジェクト2つまでのため、**既存プロジェクトを共有**する
- テーブル名は案件ごとにプレフィックスをつけて区別する
- Storageバケット名も同様にプレフィックスをつける

| 案件 | プレフィックス例 |
|------|----------------|
| Kakeru（確定申告） | `kak_` |
| Vettly（採用HR） | `vet_` |
| 次の案件 | `xxx_` |

### RLSについて
- RLSはテーブル単位で効くため、**プレフィックスが違えば別案件のデータは見えない**
- `user_id = auth.uid()` の条件さえ正しく設定されていれば完全に分離される
- プレフィックスに関係なくRLS設定自体は変わらない

### 日本語初期データの投入（重要）
- Claude Code経由でSQLを実行すると**日本語が文字化け（???）する場合がある**
- 日本語を含むINSERT文は必ず **Supabase管理画面のSQL Editor** で直接実行すること

### アクティブ年度・初期データについて
- 案件によっては初回ログイン後に初期データが必要な場合がある
- 本番では設定画面から作成できるようにする（手動SQL不要）
- 開発環境では必要に応じてSQL Editorで投入する

---

## 環境設定

### `.env.local` に必要な変数（共通）
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
RESEND_API_KEY=
ADMIN_EMAIL=
```

### AI・OCR機能を使う場合（追加）
```
OPENAI_API_KEY=
GOOGLE_CLOUD_VISION_API_KEY=
```

### Supabaseプロジェクト作成時の注意
- Regionは `Northeast Asia (Tokyo)` を選択
- 接続情報は Connect → App Frameworks タブから取得
- IPv4アドオン（有償）は不要

### Vercelデプロイ時の変更
```
NEXTAUTH_URL=https://your-project.vercel.app
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
```

---

## Next.js バージョン固有の注意事項（v16実績）

- `middleware.ts` → `proxy.ts` にリネームされている
- bcryptjsはEdge分割不要でそのまま使用可能
- Tailwind CSS v4のため `tailwind.config.ts` は使用しない
- `@theme inline` でCSS変数をTailwindユーティリティにマッピングする
- App Routerのルートグループ `(public)` `(auth)` `(app)` で認証レイアウトを分離

---

## 開発フロー

### チケット構成の推奨順序
1. デザインシステム（CSS変数・トークン定義）
2. 共通UIコンポーネント
3. DBスキーマ（Supabase MCPまたはSQL Editorでテーブル作成）
4. 認証（NextAuth）
5. レイアウト（公開・認証後）
6. ページ実装（LP → 主要機能 → AI機能 → 設定）
7. メール通知
8. デプロイ・最終確認

### サブエージェントの使用
`D:\dev\agents`（contains-studio/agents）のエージェントを活用する。

| 役割 | エージェント | 主な用途 |
|------|------------|---------|
| UI設計 | `@ui-designer` | デザイントークン・コンポーネント設計 |
| フロントエンド | `@frontend-developer` | ページ・コンポーネント実装 |
| バックエンド | `@backend-architect` | DB設計・API・RLS設定 |
| AI機能 | `@ai-engineer` | OCR・AI分類・要約機能 |
| デプロイ | `@devops-automator` | Vercel本番デプロイ |

### CLAUDE.md の基本構成
```markdown
@AGENTS.md

# CLAUDE.md

## Commands
\`\`\`bash
npm run dev      # 開発サーバー起動 (localhost:3000)
npm run build    # ビルド（デプロイ前確認）
npm run lint     # ESLintチェック
\`\`\`

No test runner is configured yet.

## Architecture
This is a Next.js 16 app using the App Router (not Pages Router).
Source lives under `app/`.

## Tech Stack
（案件ごとに記載）

## Design Tokens
（案件ごとに記載）

## Database Rules
- Prisma 不使用（IPv4環境でTCP直接接続不可）
- テーブル名はすべて {プレフィックス}_ をつける
- テーブル作成・RLS設定は Supabase MCP または SQL Editor で行う
- RLSは全テーブルに必須設定

## Known Issues & Fixes
- ボタンテキスト不可視 → CSS変数解決失敗の可能性 → HEX値で直指定
- 404エラー → App Routerフォルダ構成ズレ → ファイルパス確認
- Supabase保存エラー → RLSポリシー未設定 → MCPまたはSQL Editorで追加
- 画像アップロードエラー → Server Action上限1MB → next.config.tsで10MBに拡張
- 日本語INSERT文字化け → Claude Code経由不可 → SQL Editorで直接実行
```

---

## デザイン方針

### カラートークン
- Tailwind組み込みの `gray-*` は**使用禁止**
- カスタムカラーは `@theme inline` でCSS変数として定義
- ボタンの `background-color` はCSS変数ではなく**HEX値直書き**（デグレ防止）
  - 例：`className="bg-[#00A896] hover:bg-[#008A7C]"`
- ミュートテキストは案件ごとのサンド・グレー系カラーをHEX直書き

### 案件ごとのデザイントークン例

| 案件 | Primary | Background | Dark | 特徴 |
|------|---------|-----------|------|------|
| Kakeru | #E8884A（テラコッタ） | #F5F0E8（リネン） | #2D3B3B（ティール） | 温かみ・親しみやすさ |
| Vettly | #00A896（ティール） | #F9F8F6（オフホワイト） | #1C2B35（スレート） | スタイリッシュ・プロ |

---

## よく発生するトラブルと対処法

| 症状 | 原因 | 対処法 |
|------|------|--------|
| ボタンテキスト不可視 | CSS変数の解決失敗 | HEX値で直指定 |
| 404エラー | App Routerフォルダ構成ズレ | ファイルパスを確認 |
| Supabase保存エラー | RLSポリシー未設定 | SQL Editorで追加 |
| 画像アップロードエラー | Server Action上限1MB | next.config.tsで10MBに拡張 |
| 勘定科目・選択肢が??? | 日本語INSERT文字化け | SQL Editorで直接実行 |
| 収支登録でエラー | アクティブ年度未設定 | 設定画面または手動SQLで年度を作成 |
| 認証リダイレクトされない | proxy.tsのパス設定 | Next.js 16ではmiddleware.ts→proxy.ts |
| モバイルでログインできない | メールアドレスの大文字小文字不一致 | auth.ts の authorize関数でメールを .toLowerCase().trim() で正規化する。DBの既存データも UPDATE kak_users SET email = LOWER(email) で統一する |

---

## Supabase MCP 接続設定（2026年6月時点の正しい手順）

新規プロジェクトでSupabase MCPを使う際、**過去（Kakeru/Vettly時点）の設定方法はSupabase側の仕様変更により使えなくなっている**。新規プロジェクトでは必ず以下の新方式で設定すること。

### 起きていたトラブルの経緯（記録）

- 旧方式：`claude mcp add supabase --transport sse https://mcp.supabase.com/sse --header "Authorization: Bearer <Personal Access Token>"`
- この旧URL（`/sse`）は廃止されており、アクセスすると `404 Not Found` になる
- `claude mcp list` で `supabase: ... Failed to connect` と出たら、まずこの旧URL問題を疑うこと
- Claude Code自身に「MCP設定がない」と何度も言われても、実際はファイルに設定はあり、接続だけが失敗していることがある。`claude mcp list` で事実を確認するのが一番早い

### 正しい設定手順（新規プロジェクトごとに毎回必要）

1. **Supabase管理画面で project_ref を確認**
   - 対象プロジェクト → Settings → General → 「Project ID」をコピー

2. **ターミナルでMCPサーバーを登録**（プロジェクトのルートディレクトリで実行）
   ```powershell
   claude mcp add supabase --transport http "https://mcp.supabase.com/mcp?project_ref=<project-ref>"
   ```
   - Personal Access Tokenは**もう不要**（古い情報には書かれているが無視してよい）
   - 既に登録済みでエラーが出る場合は、先に `claude mcp remove supabase` してから再登録する

3. **Claude Codeを起動し、Supabase関連の操作を依頼する**
   ```powershell
   claude --dangerously-skip-permissions
   ```
   - 初回はOAuth認証が必要。Claude Codeが認証用URLを提示するので、ブラウザでそれを開く
   - Supabaseアカウントでログインし、組織・プロジェクトへのアクセスを許可
   - 「Authentication successful」が出たらブラウザタブを閉じてClaude Codeに戻る

4. **動作確認**
   ```
   Supabase MCPのツールが使えるか確認してください
   ```
   - `list_tables` 等が正常に返ってくればOK

### 注意点

- 古いプロジェクト（Kakeru/Vettly/ServiceHub）の `.claude.json` に残っている旧SSE設定はもう動かない。それらのプロジェクトで久々にMCPを使う際も、上記の新方式で設定し直す必要がある
- `~/.claude.json`（ホーム直下、`.claude`フォルダの中ではない）にプロジェクトパスごとのMCP設定が保存されている。Claude Code自身がこのファイルの場所を見失って「設定がありません」と誤った報告をすることがあるので、信用せず `claude mcp list` で確認すること
