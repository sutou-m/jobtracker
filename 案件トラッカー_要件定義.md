# 案件トラッカー（JobTracker） 要件定義書

## 1. 背景・目的

フリーランスとして案件を獲得するため、複数の案件サイト（indeed、ランサーズ、Findy、Wantedly）で見つけた案件情報を一元管理し、応募〜受注までの進捗を追跡するための個人用Webアプリ。

**重要：本アプリは自動スクレイピング（サイト一覧ページの自動巡回・大量取得）は行わない。**
indeed / ランサーズ / Wantedly はいずれも利用規約で自動収集（ボット・クローラー・スクレイパー等）を明示的に禁止しており、Findyも案件検索用の公式APIを提供していない。そのため本アプリは「ユーザーが手動で見つけた案件の個別URLを1件ずつ貼り付け、そのページ1件分だけを読み取る」設計とする。これは検索エンジン的な自動巡回ではなく、人間が能動的に開いた1ページの情報を補助的に整理するだけの行為であり、規約上のリスクを最小化する。

対象外サイト：クラウドワークス（クラウドワークステック）は明示的に除外。

## 2. 技術スタック（既存プロジェクトの規約に準拠）

- フロントエンド: Next.js 14（App Router）
- DB: 既存のSupabaseプロジェクトを利用（無料プランは2プロジェクト上限のため新規作成しない）
  - テーブルは `job_` プレフィックスで追加（kak_ / vet_ と同様の命名規則）
- スタイリング: Tailwind CSS v4（`@theme inline` を使用。`tailwind.config.ts` は作らない）
- デプロイ: Vercel
- 認証: なし（個人専用URLとして運用。第三者に推測されにくいURLにする程度の配慮は可）
- 開発環境: Windows / PowerShell / D:\dev\ 配下、GitHubアカウント sutou-m

## 3. 機能要件

### 3.1 案件登録
- 案件URLを貼り付けて登録
- サーバー側（Next.js の Route Handler）でURLをfetchし、HTML解析を試行
  - 取得できた場合: タイトル・概要文・想定単価などをベストエフォートで自動抽出し、フォームに仮入力
  - 取得できない場合（JS必須・ログイン必須・bot対策等で失敗した場合）: エラーを表示し、空のフォームへ素直にフォールバック。ユーザーが全項目を手入力する
  - 自動抽出はあくまで「下書きの補助」とし、登録前に必ずユーザーが内容を確認・編集できるようにする
- サイト種別はURLのドメインから自動判定してタグ付け（indeed / ランサーズ / Findy / Wantedly / その他）
- 登録項目：
  - URL（必須）
  - サイト種別（自動判定、手動変更可）
  - 案件タイトル
  - 概要・詳細メモ
  - 想定単価・報酬（自由記述でも可）
  - 必須スキル・タグ（自由入力、複数可）
  - 進捗ステータス（後述）
  - 登録日（自動）
  - 更新日（自動）

### 3.2 進捗管理
ステータスを以下の固定値で管理し、一覧でフィルタ・カンバン的に表示できるようにする：

1. 未応募（デフォルト）
2. 応募済
3. 面談
4. 受注
5. 見送り

ステータス変更履歴は必須ではないが、更新日時は自動更新する。

### 3.3 一覧・検索
- 案件一覧をカード or テーブルで表示
- サイト種別・ステータス・タグでフィルタ
- タイトル・メモのキーワード検索
- 新着順・更新順のソート

### 3.4 削除・編集
- 登録済み案件の編集・削除が可能

## 4. データベース設計（案）

```sql
create table job_postings (
  id uuid primary key default gen_random_uuid(),
  url text not null,
  source text not null, -- 'indeed' | 'lancers' | 'findy' | 'wantedly' | 'other'
  title text,
  summary text,
  rate text,
  tags text[],
  status text not null default 'not_applied', 
    -- 'not_applied' | 'applied' | 'interview' | 'won' | 'declined'
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

RLSについては個人専用利用のため必須ではないが、将来的にSupabase Authを追加する可能性を考慮し、`user_id` カラムを最初から用意しておくことを推奨（今回はNULL許容・未使用でも良い）。

## 5. 自動取得の実装方針（重要・スコープを明確に）

- 取得対象は **ユーザーが入力した1件のURLのみ**。サイト内のリンクを辿ったり、検索結果ページを巡回したりする実装は一切行わない
- サーバー側 fetch は通常のブラウザに近いUser-Agentを使い、過度なリトライや高頻度アクセスはしない（1ユーザー操作につき1リクエストのみ）
- HTML解析は `<title>` タグ、OGPメタタグ（og:title, og:description）など、一般的にクローラーへの公開を前提とした軽量なメタ情報の抽出に留める。ログイン必須ページや動的レンダリング（CSR）が必要なページは取得失敗として扱い、手入力に自然にフォールバックする
- 取得に失敗してもアプリがエラーで止まらないよう、try-catchで握りつぶしてフォームを空のまま表示する

## 6. 画面構成（最小構成）

1. `/` 一覧画面（フィルタ・検索・ステータスバッジ表示）
2. `/new` 案件登録画面（URL入力→自動取得ボタン→フォーム編集→保存）
3. `/jobs/[id]` 詳細・編集画面

## 7. 非機能要件・注意事項

- スマホからも使えるレスポンシブ対応（外出先で見つけた案件をすぐ登録したいため）
- Supabase無料プランの制約（プロジェクト数上限など）を踏まえ、既存プロジェクトのテーブルを汚さないよう `job_` プレフィックスを徹底する
- 日本語データをSQL経由でINSERTする際の文字化け対策として、初期シードデータの投入はSupabase SQL Editorから直接行う（Claude Code経由のCLI実行は避ける）
- ボタン文字色は既存プロジェクトの教訓に従い、Tailwindの動的クラスではなくHEXリテラルで明示指定する

## 8. 今後の拡張候補（初期スコープ外）
- ブラウザ拡張機能・ブックマークレットで「このページを登録」をワンクリック化
- 案件と実際の提案文・見積もりのひも付け管理
- 受注後はMillSheet Proのような実案件管理ツールとの連携
